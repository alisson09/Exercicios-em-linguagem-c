#include <stdio.h>
#include <stdlib.h>

int main (){
    int l,area;
    printf("Digite o valor do lado de um quadrado: ");
    scanf("%d",&l);
    area = l*l;
    printf("a area do quadrado e = %d",area);

    return 0;
}
