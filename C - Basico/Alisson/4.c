#include <stdio.h>
#include <stdlib.h>

int main (){
    float x,y;
    printf("Digite um numero real: ");
    scanf("%f",&x);
    y=x*x;
    printf("O quadrado do numero digitado e %.2f",y);

    return 0;
}
