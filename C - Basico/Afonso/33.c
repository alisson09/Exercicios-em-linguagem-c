#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
    int lado;
    printf("Lado do quadrado: ");
    scanf("%d",&lado);
    printf("Area: %d",(lado*lado));
    return 0;
}
